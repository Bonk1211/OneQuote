-- Seed data for development/testing
-- Note: In actual Supabase, users are created via auth.users first.
-- This seed assumes a test user has been created with the following UUID.
-- Run after setting up Supabase Auth.

-- Example: insert test overheads for manual testing
-- INSERT INTO public.base_overheads (user_id, insurance_monthly, vehicle_monthly, ...) VALUES (...);
